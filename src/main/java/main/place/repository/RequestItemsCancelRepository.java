package main.place.repository;

import main.place.entity.RequestItemsCancel;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RequestItemsCancelRepository extends JpaRepository<RequestItemsCancel, Integer> {

}
